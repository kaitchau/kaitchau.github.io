import '../stylesheets/Contact.css';

export default function Contact() {
    return (
        <div id='contact' className='contact-container'>
            <p>Hi from contact area</p>
        </div>
    );
}