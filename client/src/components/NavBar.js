import '../stylesheets/NavBar.css';

export default function NavBar() {
    return(
        <nav className='nav-bar'>
            <a href='#'>Home</a>
            <a href='#'>About</a>
            <a href='#'>Contact</a>
        </nav>
    );
}